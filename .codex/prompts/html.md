---
description: "Generate a reading-first self-contained HTML artifact from current complex output"
argument-hint: "[optional topic or content]"
---

Use the `html-artifact` skill. Read the installed `html-artifact/SKILL.md` if it is not already loaded, then turn the current complex output or the provided argument into a self-contained HTML artifact.
